# Multi-surface audit — verified findings


## catalog-listing  (/aixcelerator/agents)
Files: src/pages/aixcelerator/agents.tsx, src/components/AgentCard.tsx
specSound: **true** · confirmed P1:3 P2:6 P3:2 · dropped:0

**Top P1 fixes:**
1. Remove forbidden amber from the Beta status pill: replace STATUS_TONES.beta (catalogFormatters.ts:35-37) with DEFAULT_STATUS_TONE (zinc --neutral-* tokens), and add a sr-only 'Status:' prefix + keep the visible label in AgentCard so zinc-only pills still convey state (fixes P1-amber + absorbs the color-only a11y concern). Regression-check all catalog card types since the formatter is shared.
2. Remove green/--trusted-* from the Live/active status pill (catalogFormatters.ts:31-34 → DEFAULT_STATUS_TONE) AND the Verified badge in AgentCard.tsx:78 (swap --trusted-surface/text/stroke → --neutral-fill/text/stroke, keep the checkmark glyph as the non-color signal). Do it at the string/component level, never by repointing the global --trusted-* vars.
3. Fix the animation composition bug at agents.tsx:487: drop 'reveal' so the grid is className='stagger-grid mt-6 grid gap-4 sm:mt-8 sm:grid-cols-2 lg:grid-cols-3'; the grid is already a top-level sibling so the IntersectionObserver stagger fires correctly per the components/CLAUDE.md rule.

**Confirmed P1 detail:**
- `P1-brand-status-amber` — catalogFormatters.ts:35-37 STATUS_TONES.beta = literal amber-50/amber-700/amber-200/dark:amber-900/30/amber-200/amber-500/40; getStatusTone (line 42-43) feeds AgentCard.tsx:64 status pill. amber-* is explicitly FORBIDDEN by locked standard.
- `P1-brand-green-status` — active/live STATUS_TONES (catalogFormatters.ts:31-34) use --trusted-* = green #166534 light / #86EFAC dark (globals.css 41-43, 173-175); AgentCard verified badge (line 78) also uses --trusted-*. Both render green on this surface, violating 'no green status'. Note: getVisibilityTone is NOT used on this page (AgentCard/agents.tsx never call it), so the finding overstates that path — the real leaks are getStatusTone(active/live) + the verified badge.
- `P1-comp-reveal-stagger-conflict` — agents.tsx:487 className='reveal stagger-grid mt-6 grid ...' combines both on one element; components/CLAUDE.md animation rule states NEVER nest stagger-grid inside .reveal because the IntersectionObserver only observes top-level nodes. Genuine bug; fix is removing 'reveal' (grid is already a top-level sibling).

**Confirmed P2:**
- `P1-a11y-status-coloronly-and-contrast` — Status pill differentiation is color-tone-only (text label is the SAME zinc weight regardless of state) — WCAG 1.4.1 risk is genuine. Once amber/green are removed, all pills become zinc and the color signal vanishes entirely, so a sr-only 'Status:' prefix + the visible label is the correct fix. Contrast claim: --neutral-text on --neutral-fill passes; the amber-dark (amber-200 on amber-900/30) is the low-contrast case and is removed by P1-amber anyway. Downgrade to P2 — it is dependent on/absorbed by the two color P1s, not an independent shipping blocker.
- `P2-ia-filter-overload-no-clear` — Filter panel renders search + up to 5 selects (industry/status/source/department/tag, lines 282-427) + 3 sort chips + optional 3 visibility chips, no Clear-all, and the empty StatePanel (line 495) passes NO action prop while its copy says 'Try clearing filters'. Hick's/Tesler's genuinely bite. P2 correct.
- `P2-hier-stat-pile` — Hero stacks SectionHeader+MiniOntologyDiagram (241-258), then AeoQuickAnswer (260), CatalogSnapshot (266), then surface-panel with its OWN redundant SectionHeader 'Search and filter' (275-280) before any results. The redundant inner SectionHeader is the concrete, fixable part. Valid P2 but partly subjective; keep P2.
- `P2-brand-hardcoded-dot-hex` — AgentCard.tsx:46 bg-[#DC2626] is hardcoded; --pivot-fill is #DC2626 light / #F87171 dark (globals 38,171). So the dot stays over-saturated coral in dark mode and bypasses the token contract. Fix bg-[var(--pivot-fill)] is exact and composes an existing token. P2 correct.
- `P2-a11y-input-tokens-focus` — Search input + all 5 selects (lines 297,330,351,372,394,417) use ad-hoc focus:ring-2 focus:ring-zinc-900/10 (low-opacity ~2px) instead of the site-standard 3px box-shadow var(--stroke-focus) used by .focus-ring and a/button:focus-visible (globals 1329-1339). The custom ring's 10% opacity likely fails the 3:1 focus-indicator floor. Real WCAG 2.4.7/consistency issue. P2 correct.
- `P2-a11y-sort-tablist-semantics` — Sort chips (442-457) and visibility chips (464-479) use aria-pressed as a single-select group with no role=group/radiogroup and 'Sort' is a bare <span> (line 430-432) not programmatically tied. Genuine 1.3.1 gap. Note: these are toggle BUTTONS, not tabs — proposing role=radiogroup/radio (or group+aria-labelledby) is appropriate; do NOT impose tablist semantics. P2 correct.

_Spec caveats:_ Spec is overwhelmingly sound — every edit composes EXISTING tokens (--neutral-fill/text/stroke, --pivot-fill, --stroke-focus, --surface-soft) and existing classes (.stagger-grid, .surface-panel, .chip/.chip-brand, .btn/.btn-secondary/.btn-sm, .focus-ring), invents no new color, stays zinc+coral/Inter, and respects .catalog-card/page-structure rules. Caveats for the implementer: (1) The proposed new .form-control class is acceptable (it only composes existing tokens and matches the existing focus-visible pattern at globals 1329-1339), but it is genuinely NEW CSS — verify it does not collide with the existing input/select base styles already at globals.css ~283-296 (.dark input focus border uses --stroke-focus) and apply it additively so responsive grid spans (lg:col-span-*) survive. (2) The SOURCE_TONES.partner violet edit and the getVisibilityTone edit are DEAD-CODE cleanup on THIS surface — AgentCard never calls getSourceTone or getVisibilityTone (it hardcodes Internal/External chips inline, lines 68-76), so neither violet nor that green path actually renders on /aixcelerator/agents. Keep the edits for global hygiene but do not claim they fix a visible /agents defect, and check MCPCard/other cards before deleting, since catalogFormatters is shared. (3) Editing STATUS_TONES.active/live at the formatter-string level (NOT the global --trusted-* CSS vars) is the correct low-blast-radius choice — the spec's risk note already flags that --trusted-* still powers .badge-verified/.status-* utilities elsewhere; leave those vars untouched. (4) AgentCard verified-badge swap to --neutral-* is a per-component change (AgentCard only) and is safe; MCPCard/SkillCard verified badges, if any, are separate.

_Blockers:_
- catalogFormatters.ts is SHARED across AgentCard, MCPCard, SkillCard and the catalog cards (per file header + lib/CLAUDE.md). Changing STATUS_TONES.beta/active/live and SOURCE_TONES.partner will recolor status/source pills on EVERY catalog card type, not just /agents. This is the intended fix (purge forbidden colors globally) but the implementer must regression-check the mcp/skills/tools/use-cases listing + detail pages in BOTH modes before merge.
- Do NOT touch the global --trusted-* / --pivot-* / --neutral-* CSS variables in globals.css — they power .badge-verified, .status-*, .pivot-* utilities used across many pages. All color fixes must happen at the formatter-string / per-component class level (as the spec correctly scopes).
- getVisibilityTone and getSourceTone are exported and may be consumed by other surfaces (MCP/visibility pills) even though /agents does not use them — grep all callers before editing those functions so a /agents-driven change does not silently alter another page.


## detail-profile  (/aixcelerator/agents/[slug])
Files: src/pages/aixcelerator/agents/[slug].tsx
specSound: **true** · confirmed P1:1 P2:3 P3:3 · dropped:0

**Top P1 fixes:**
1. StickyTabBar a11y (P1-tab-semantics-keyboard): add aria-current to active tab, pair coral border with font-semibold (non-color cue), and move focus to the target section after scrollIntoView (tabIndex=-1 + focus({preventScroll:true})). This is the only confirmed true-P1 and fixes WCAG 1.4.1/2.4.3/4.1.2.
2. Mobile contentIa (downgraded to P2 but highest-leverage remaining): surface the genuinely-lost Tags + Adoption panels in the main column at <1024px via a shared .surface-panel fragment — do NOT duplicate Status/Source/Department/CTA that hero+Specs+EnterpriseCtaBand already render on mobile.
3. Remove the forbidden .card-elevated wrapper on Related Agents (L598) — render <AgentCard agent={related} /> bare to eliminate the hover-lift, decorative shadow, and double border, matching the flat catalog standard used on listing pages.

**Confirmed P1 detail:**
- `P1-tab-semantics-keyboard` — StickyTabBar L44-63: buttons set activeId/scrollIntoView only; no aria-current, active state is color-only (border-[#DC2626] + text shade), and no focus is moved to the target section. Confirmed WCAG 1.4.1/2.4.3/4.1.2 gaps. Global button:focus-visible ring (globals.css L1334-1339) exists but does not fix the missing programmatic state or focus move.

**Confirmed P2:**
- `P1-sidebar-desktop-only` — L608 aside is `hidden lg:block` so Tags + Adoption panels and the sticky CTA pair vanish <1024px. BUT the claim is partly overstated: hero (EnterprisePageHero) renders a primaryAction CTA + Status/Department/Source metrics, and the bottom EnterpriseCtaBand renders a Book-a-demo CTA — both visible on mobile. The Specs section (L526-544) re-renders Status/Industry/Source/Department/Verified. So Status/Source/Department AND a CTA DO survive on mobile; only Tags and Adoption signals are genuinely lost.
- `P2-card-elevated-hover-lift` — Confirmed: [slug].tsx L598 wraps AgentCard in `.card-elevated`; globals.css L2032-2058 applies box-shadow + hover translateY(-2px)+shadow-lg — the forbidden hover-lift/decorative-shadow. AgentCard.tsx L43 already renders `.catalog-card` (bordered), so this double-borders. Genuine system violation.
- `P2-coral-not-token-swapped-dark` — Confirmed hardcoded bg-[#DC2626]/text-[#DC2626]/border-[#DC2626] at L290,309,365,450,458,735 with no dark variant; globals.css L38/L171 prove --pivot-fill swaps #DC2626→#F87171 in dark, and the page's own [&_a] anchors swap (L777). So these accents stay #DC2626 in dark, off-spec vs the rest of the system.

_Spec caveats:_ Every proposed edit composes existing tokens/classes (.surface-panel, --pivot-fill, --site-header-height, .chip-neutral, .btn-primary/.btn-secondary, font-medium/semibold, text-zinc-400/500) and invents no new values; newGlobalCssNeeded=false is correct. Caveats: (1) The P1 sidebar fix proposes rendering Quick Stats + Tags + CTA in a `.surface-panel` block in the main column at <1024px. That is sound, but the audit's framing that 'CTAs never render' on mobile is inaccurate — the hero primaryAction and bottom EnterpriseCtaBand already provide a mobile CTA, and Specs already re-renders Status/Source/Department/Verified. To avoid REDUNDANT data on mobile, the mobile clone should surface only what is actually lost (Tags + Adoption), not duplicate Status/Source/Department that the hero+Specs already show. Spec should be narrowed accordingly. (2) scroll-mt fix `calc(var(--site-header-height)+3rem)` = 64px+48px=112px, which is LESS than the current 128px; that is closer to the true sticky stack (64px header + ~48px tab bar) so it is an improvement, but the dev must confirm the tab bar's real rendered height rather than assuming 3rem. (3) The StickyTabBar tabindex=-1 + focus({preventScroll:true}) edit is correct but the component is shared — see blockers. (4) aria-current='location' is valid; pairing with font-semibold for a non-color cue is on-system. No edit regresses dark mode or leaves the monochrome+coral/Inter system.

_Blockers:_
- StickyTabBar.tsx is a SHARED component — verify which other detail/listing routes consume it before changing markup/behavior (focus-move + aria-current changes affect every consumer). Grep usages before editing.
- The mobile metadata clone must be a single shared JSX fragment rendered in both the lg:hidden main-column block and the lg:block aside — copy-paste duplication would drift. Extract a local component/fragment in [slug].tsx.
- Section focus-move (tabIndex=-1 + el.focus) must use :focus-visible (not :focus) so large section containers don't paint a persistent outline on click; rely on the existing global a/button:focus-visible ring, do not add a new outline.
- scroll-mt value change depends on the actual rendered StickyTabBar height; if the tab bar wraps or changes height the calc must track it — confirm before hardcoding 3rem.


## ontology  (/aixcelerator/agents/ontology)
Files: src/components/OntologyPageTemplate.tsx
specSound: **false** · confirmed P1:3 P2:3 P3:1 · dropped:1

**Top P1 fixes:**
1. P1-svg-keyboard — make every clickable <g> a real control (role='button', tabIndex=0, aria-label, onKeyDown Enter/Space firing the same router.push), drive tint off focus as well as hover, add a 2px cl.textPrimary SVG focus ring, and give the <svg> role='img'+aria-label (or figure/figcaption). This is the surface's primary task and is currently entirely mouse-only and unlabeled.
2. P1-contrast-svg — in svgColors() raise the muted ramps to clear 4.5:1: light textTertiary #a1a1aa->#71717a, light textSecondary #71717a->#52525B (the documented --text-muted 7:1), dark textTertiary #71717a->#a1a1aa; apply the same lift to the text-[9px] zinc-400/500 mobile labels (128/135/152/165).
3. P1-touch-targets — bump the mobile category/relation/collection <button>s to min-h-[44px] px-3 text-[11px]/text-xs and tag chips to min-h-[28px], keeping >=24px gaps; the global a/button focus-visible ring already applies, just verify it is not clipped by overflow.

**Confirmed P1 detail:**
- `P1-svg-keyboard` — Confirmed: clickable category <g> (286-292), item <g> (366-372), collection <g> (411-417) are bare onClick + style cursor:pointer + onMouseEnter/Leave, with NO tabIndex/role/onKeyDown/focus and no <title>/aria-label; <svg> (232) has no role/aria-label. Core task is fully mouse-only and unlabeled.
- `P1-contrast-svg` — Confirmed: svgColors() sets light textTertiary '#a1a1aa' (zinc-400, ~2.0:1 on surfaceAlt #fafafa) used on layer headers (244/327/400), '{total} total' (252), category counts (302), legend (336/431); textSecondary '#71717a' (~3.9:1) on collection counts (425) and legend (440/446); dark textTertiary '#71717a' on #27272a ~3.0:1. All <12px so 3:1 large-text allowance N/A. Genuine AA fail in both modes.
- `P1-touch-targets` — Confirmed in isMobile branch (<640px is the sole nav path): category buttons px-2 py-1 text-[10px] (~24px, 124-130), tag chips px-1.5 py-0.5 (135), relation buttons px-2 py-1 (146), collection p-2 (163) — all under the 44px AAA and at/under the 24px AA floor. Hard target-size failure.

**Confirmed P2:**
- `P2-nested-reveal` — Confirmed: hero div className='reveal' (498) wraps SectionHeader which emits its own .reveal classes (44-65) — exactly the banned single-level-IntersectionObserver nesting per components/CLAUDE.md; same at sections 537/629 with SectionHeaders 538/630. Diagram column (523) has no reveal so halves animate inconsistently. animate={false} prop exists (SectionHeader 13/25) so the proposed fix is valid.
- `P2-shadow-decoration` — Confirmed: hero callout has shadow-sm dark:shadow-zinc-950/30 (507) and diagram container has shadow-sm (523); .catalog-card (globals 2261) and .surface-panel (561) are box-shadow:none. These are the only shadowed surfaces on the page, inconsistent with the locked flat-card standard.
- `P2-hardcoded-coral-hex` — Confirmed all cited literals: bg-[#DC2626]/10 dark:bg-[#DC2626]/15 + text-[#DC2626] dark:text-[#F87171] (509-513), borderLeft '3px solid #DC2626' inline (540/568/596), SVG fills/strokes #DC2626 in both modes (376-377/419-420), Explore CTAs text-[#DC2626] dark:text-[#F87171] (658/666/674). The SVG coral genuinely does NOT flip to #F87171 in dark. NOTE: text-[#DC2626] dark:text-[#F87171] is already correct in dark for the className cases, so only the bg chip and the SVG-fill cases are the real dark-mode regressions; the borderLeft inline #DC2626 also does not flip.

**Dropped/refuted:** P3-svg-transition-reduced-motion→drop

_Spec caveats:_ Mostly sound and on-system (all edits stay zinc+coral/Inter, compose .surface-panel / SectionHeader animate prop / global focus-visible, no new globals.css), but three concrete inaccuracies must be corrected before implementation: (1) TOKEN MISMATCH — the spec says swap the hero icon chip bg-[#DC2626]/10 to bg-[color:var(--pivot-surface)] with 'No visual change in light mode'. Wrong: --pivot-surface is #FEF2F2 (light) / #1C1412 (dark), a pale-pink/near-black solid surface, NOT a 10% coral tint over zinc-50, so the chip WILL shift visually. If exact look must be preserved, keep bg-[#DC2626]/10 and only fix the dark variant to dark:bg-[#F87171]/15, or accept the --pivot-surface look intentionally. (2) The spec's claim that text-[#DC2626] dark:text-[#F87171] 'keeps the LIGHT coral in dark mode' is FALSE for the className cases (509-comment/513/658/666/674) — those already have the dark:text-[#F87171] override and flip correctly; the only genuine dark-mode coral bug is the SVG presentation-attribute fills (376-377/419-420 literal #DC2626 in both modes) and the inline borderLeft (540/568/596). Scope the coral-token edit to those, not the already-correct className pairs. (3) Contrast remediation values are sound (light textTertiary->#71717a ~4.6:1, light textSecondary->#52525B/--text-muted 7:1, dark textTertiary->#a1a1aa) and the introduced cl.accent (isDark?#F87171:#DC2626) for SVG is correct and necessary since SVG fill cannot read var(). Everything else (role/tabIndex/onKeyDown on <g>, min-h-[44px] mobile, animate={false}, drop shadows) composes existing tokens correctly.

_Blockers:_
- SHARED TEMPLATE: OntologyPageTemplate.tsx backs 5 ontology routes (agents/skills/mcp/tools/podcasts per src/pages/CLAUDE.md route map). Every edit must be visually re-verified on all 5 in both light and dark; the contrast and coral-token changes will alter rendered pixels across all of them.
- Skills ontology is a documented EXCEPTION (custom inline OntologyDiagram, NOT this template per pages/CLAUDE.md) — do not assume fixes here propagate to /aixcelerator/skills/ontology; that route needs a separate parallel pass.
- SVG focus-ring + role=button on <g> can double-announce with a verbose <svg> aria-label; keep node labels terse and the svg label a one-line summary, and verify focus ring is not clipped by the overflow-x-auto wrapper (231) or the min-w-[580px] svg.
- READ-ONLY constraint honored — no edits/build/dev-server run; contrast ratios asserted from token math (zinc-400/#a1a1aa on #fafafa ~2.0:1, zinc-500/#71717a ~3.9:1, dark #71717a on #27272a ~3.0:1), not a live audit tool.


## graph  (/aixcelerator/agents/graph)
Files: src/components/GraphPageTemplate.tsx
specSound: **true** · confirmed P1:3 P2:3 P3:4 · dropped:0

**Top P1 fixes:**
1. P1-4: Add a keyboard/SR path — render a focusable next/link node directory (h2 + ul of nodes labeled name+category+relation count) adjacent to the canvas, give the canvas container role and aria-label, and replace window.location.href (115) with router.push. This unblocks the surface's primary job for keyboard/SR users (WCAG 2.1.1/1.1.1).
2. P1-3: Bump ctrlBtn to .btn-icon (44px) and ctrlBtnWide/edge chips to min-h-[2.75rem], add aria-label to every icon button (Zoom in/out, Reset view, Fullscreen), label the search input via aria-label, and add explicit focus-visible rings on the dark control surface (WCAG 2.5.8/4.1.2/2.4.7, Fitts).
3. P1-1: Stop the forbidden rainbow hexes from rendering in DOM — recolor the edge-filter dot (232), tooltip dot (379), legend dots (406) and legend lines (421) to the zinc scale (ideally via bg-zinc-{600..300} Tailwind classes, not inline hex), reserving coral only for the active/highlighted relation, while leaving CATEGORY_COLORS/RELATIONSHIP_TYPE_COLORS for canvas-internal use only.

**Confirmed P1 detail:**
- `P1-1` — Confirmed: graphUtils.ts:39-98 defines forbidden hexes (#60a5fa blue, #34d399 emerald, #fbbf24 amber, #a78bfa violet, #f472b6 pink, #fb923c orange, #38bdf8 sky, #94a3b8 slate) and they are rendered as inline-style backgroundColor in DOM at lines 232 (edge-filter dot), 379 (tooltip dot), 406 (legend dot), 421 (legend line) — these are page DOM chips, not canvas, so a genuine locked-color violation.
- `P1-3` — Confirmed: ctrlBtn h-8 w-8 (165)=32px, ctrlBtnWide h-8 (166)=32px, edge chips px-2.5 py-1.5 text-[10px] (224)~26px — all below 44px; every icon button (245-255) has only title= no aria-label (4.1.2); search input (199-206) has no label, placeholder-only (1.3.1/3.3.2). Real WCAG 2.5.8 + 4.1.2 + Fitts failures on the page's primary tools.
- `P1-4` — Confirmed: node info only on onNodeHover (290), nav via window.location.href in onNodeClick (115), canvas not focusable, no keyboard/SR path to nodes, tooltip (377-393) not announced. The surface's primary job is inaccessible to keyboard/SR users — real WCAG 2.1.1/1.1.1/4.1.2.

**Confirmed P2:**
- `P1-2` — Confirmed: backgroundColor="#09090b" hardcoded (288), overlay bar bg-zinc-900/70 (195), ctrlBtn/ctrlBtnWide constant zinc-800 (165-166), search input + stats badge constant dark (205,362) with no dark: pairing. Note container frame at 170 IS theme-aware (border-zinc-200/80 + dark:border-zinc-700/60), so finding is slightly overstated but the canvas+controls being constant-dark in light mode is real.
- `P2-1` — Confirmed: linkDirectionalParticles returns 3 (282-284), particle speed 0.004 (285) runs continuously; CSS prefers-reduced-motion block (2650-2678) only neutralizes .reveal/.stagger-grid/.rise-in and CSS durations and cannot reach the JS react-force-graph particle loop. Real WCAG 2.3.3.
- `P2-3` — Confirmed: overlay control bar is ad-hoc zinc utilities not .surface-panel (195-257); legend is bespoke rounded-xl bg-zinc-50 (397) not .surface-panel; edge chips reimplement styling inline (224-237) instead of composing existing .chip-neutral (355) / .chip-brand (325). Real page-structure-standard deviation.

_Spec caveats:_ Spec is largely implementable and composes real tokens, with these caveats: (1) .btn-icon is confirmed real (globals.css:1462, height/width 2.75rem=44px) and .chip-neutral/.chip-brand/.surface-panel all exist and are box-shadow:none — good. (2) The proposed domSwatchColor helper hardcodes hex literals (#52525B/#71717A/#A1A1AA/#D4D4D8). Those values ARE the zinc-600/500/400/300 token values so they stay on-system, but the locked rule says do not hardcode a hex a token already expresses — prefer rendering the swatches via Tailwind bg-zinc-{600..300} classes (or CSS vars) rather than inline-style hex to fully honor the token rule. The 'even simpler: make all DOM swatches zinc and rely on text label' alternative is the cleaner, more compliant path. (3) Spec says compose `chip ${active?'chip-brand':'chip-neutral'}` but the base `.chip` min-height is 28px (309), so the explicit min-h-[2.75rem]/py-2 bump the spec adds is required to reach 44px — correctly noted. (4) Constant-dark canvas with the bg-zinc-900/80 text-zinc-100 border-white/10 control pills is acceptable since the canvas is constant-dark in both modes; border-white/10 is an opacity utility, on-system. (5) The 'recolor nodes/edges/labels for light' branch (label fill #e4e4e7 at 331) is correctly flagged as out-of-scope risk; recommended path keeps canvas constant-dark — sound. (6) react-force-graph is typed any (98 etc.), so the linkDirectionalArrowLength swap for reduced-motion must be verified against the installed version — spec already flags this. No invented global CSS (newGlobalCssNeeded:false is correct; all needed classes already exist).

_Blockers:_
- GraphPageTemplate.tsx is shared by all 5 content-type graph pages (skills/mcp/agents/tools/podcasts /graph) per the file header — every edit ships to all five surfaces simultaneously, so changes must be validated across all configs, not just agents.
- graphUtils.ts CATEGORY_COLORS and RELATIONSHIP_TYPE_COLORS are consumed by the canvas here AND by ontology diagrams, collection-detail mini-graphs, and CollectionGraph (per file header comment). Do NOT mutate those exported maps; the spec's add-new-domSwatchColor-helper approach is mandatory to avoid regressing other surfaces.
- react-force-graph-2d props are untyped (any). The reduced-motion swap from linkDirectionalParticles to linkDirectionalArrowLength must be confirmed supported by the installed version before relying on it for directionality.
- Focus-visible rings on overlay controls sit inside the rounded-2xl overflow-hidden container (170); the global a/button:focus-visible box-shadow (globals.css:1335) may be clipped — the explicit focus-visible:ring on the controls must be tested so it is not cut off.


## collections-listing  (/aixcelerator/agents/collections)
Files: src/components/CollectionsPageTemplate.tsx
specSound: **true** · confirmed P1:1 P2:3 P3:4 · dropped:0

**Top P1 fixes:**
1. Search input accessibility+contrast (P1-search-no-label-contrast): wrap in <form role="search" onSubmit preventDefault>, add type="search" + visually-hidden <label htmlFor>, and raise placeholder-zinc-400→placeholder-zinc-500 dark:placeholder-zinc-400 to clear 4.5:1. The only genuine WCAG AA failure on the surface.
2. Remove the `.reveal`+`.stagger-grid` collision on line 203 (drop `.reveal`, keep `stagger-grid mt-6 grid ...`): documented-forbidden in both CLAUDE.md files. Not the usability catastrophe the audit claims, but the cheapest way to satisfy the locked animation contract and de-risk a future lint/observer change.
3. Expose filter + result state to AT and bump control sizes (merge P2-filter-state-not-exposed + P1-target-size-controls): add aria-pressed to the All + category toggles, aria-live="polite" on the count <p> (line 195), route pills through `.chip chip-brand`/`.chip chip-neutral`, and size search to h-11 (44px) with min-h-[44px] pills.

**Confirmed P1 detail:**
- `P1-search-no-label-contrast` — Confirmed: input (lines 153-159) has no <label>, no role=search, no aria-label, no type=search; placeholder-zinc-400 (#A1A1AA) on bg-zinc-50 (#FAFAFA) ≈2.5:1, fails WCAG 1.4.3 (4.5:1). Genuine AA failure + missing programmatic label (1.3.1/3.3.2). Correctly P1.

**Confirmed P2:**
- `P1-target-size-controls` — Confirmed: input is h-9=36px (line 158); pills are px-3 py-1.5 text-xs ≈28-30px (lines 163,175); `.chip` base is min-height:28px (globals.css:309). Real Fitts/comfort issue, but WCAG 2.5.8 AA floor is 24px (these pass it); 44px is the AAA 2.5.5 target the audit itself concedes. Design-consistency/Fitts, not an AA failure.
- `P2-filter-state-not-exposed` — Confirmed: toggle buttons (lines 161-183) expose active state only via coral text/ring with no aria-pressed; result-count <p> (line 195) has no aria-live, so filtering is silent to AT. Real 4.1.2 + 1.4.1 gap.
- `P2-locked-chip-classes-bypassed` — Confirmed: lines 165/177 hardcode `bg-[#DC2626]/10 text-[#DC2626] ring-1 ring-[#DC2626]/20` and lines 166/178 `bg-zinc-100 text-zinc-600` instead of the locked `.chip-brand`/`.chip-neutral` (globals.css:325/355); card tag line 59 hardcodes `bg-[#DC2626]/10 ... dark:text-[#F87171]` instead of `--pivot-surface`/`--pivot-fill`. Real design-system drift; tokens/classes all exist.

_Spec caveats:_ The spec is overwhelmingly sound — every proposed edit composes classes/tokens that I verified exist (`.chip`/`.chip-brand`/`.chip-neutral` globals.css:299-407, `.btn-secondary` 1440, `.card-arrow` 2287, `--pivot-surface`/`--pivot-fill` 35/38/168/171, `--text-muted` 73/195), stays on the locked monochrome+coral/Inter system, and adds NO new global CSS. Issues: (1) The collision edit's RATIONALE is overstated ('can leave cards stuck at opacity:0') — verified false against the single Layout observer; the edit itself (drop `.reveal` from the grid) is still correct and worth doing to satisfy the documented contract, just not for the stated reason. (2) The target-size edit prescribes `min-h-[40px]` for pills, an arbitrary value that does NOT reach the 44px Fitts target the finding invokes — should be `min-h-[44px]` (or `h-11`) for internal consistency. (3) Note `.chip-neutral` resting bg is #F4F4F5 (globals.css:357), which equals Tailwind zinc-100, so swapping inactive pills to `.chip-neutral` is a visual no-op in light mode and safe; in dark it shifts zinc-800→#27272A (zinc-800) — also effectively identical, so no regression. (4) Wrapping controls in `<form role=\"search\">` correctly flags the needed `onSubmit preventDefault` in risks — good. No invented tokens, no forbidden colors, no font changes.

_Blockers:_
- SHARED TEMPLATE: CollectionsPageTemplate.tsx renders 5 routes (skills/mcp/agents/tools/podcasts collections per file header line 4). Every edit must be visually + a11y-verified across all 5 content types and both modes at 1280/1024/768/375, not just agents.
- The `reveal stagger-grid` combo is ALSO live at src/pages/aixcelerator/agents.tsx:487 (and is a repeated repo habit). Fixing only this template leaves the documented anti-pattern elsewhere; if a lint rule is added it will flag agents.tsx too. Coordinate, or scope the fix explicitly to this file.
- Changing card <h2>→<h3> assumes SectionHeader as=h1 is the only h1 on these pages — verified true here, but Layout/EnterpriseCtaBand headings should be re-confirmed per route to avoid skipped levels.


## collection-detail  (/aixcelerator/agents/collections/[slug])
Files: src/components/CollectionDetailTemplate.tsx
specSound: **true** · confirmed P1:3 P2:4 P3:1 · dropped:0

**Top P1 fixes:**
1. P1-legend-forbidden-colors: stop painting RELATIONSHIP_TYPE_COLORS emerald/amber/blue/violet/orange hexes as DOM legend swatches (CollectionGraph.tsx L188-191); replace with a monochrome .chip-neutral count pill (RELATIONSHIP_TYPE_LABELS + count) so DOM chrome is zinc+coral only and not color-only — keep canvas edge colors as the sanctioned in-canvas exception.
2. P1-heading-as-caption: promote the three section <h2>s (CollectionDetailTemplate.tsx L127/L148/L178) from 11px zinc-500 uppercase kickers to real headings via SectionHeader as='h2' size='md' animate={false}, restoring H1>H2>body rank.
3. P1-grid-not-staggergrid: make the items grid its own top-level .stagger-grid (sibling of, not nested inside, the .reveal at L147), wrapping only the heading in .reveal, so per-card staggered entrance works and the no-nest animation rule is honored.

**Confirmed P1 detail:**
- `P1-legend-forbidden-colors` — CONFIRMED: CollectionGraph.tsx L188-191 sets style={{backgroundColor: RELATIONSHIP_TYPE_COLORS[type]}} on DOM <span> swatches; graphUtils.ts L79-97 are literal forbidden hexes (#34d399 emerald, #fbbf24 amber, #60a5fa blue, #a78bfa violet, #fb923c orange). This is HTML chrome below the canvas, not the in-canvas exception, and is color-only (swatch+label differ only by hue) violating WCAG 1.4.1.
- `P1-heading-as-caption` — CONFIRMED: L127, L148, L178 are real <h2> styled text-[0.6875rem] uppercase text-zinc-500 — an 11px micro-kicker rendered as the section heading, smaller than the body it introduces. No SectionHeader present; collapses H1>H2>body rank.
- `P1-grid-not-staggergrid` — CONFIRMED: items section L147 is .reveal and grid L151 is plain 'grid gap-4 sm:grid-cols-2 lg:grid-cols-3' with no .stagger-grid. Violates page-structure standard #3 and the documented animation rule (CLAUDE.md: never nest stagger in .reveal; IntersectionObserver only observes top-level). Cards get no per-card entrance.

**Confirmed P2:**
- `P1-hero-not-standard` — CONFIRMED: L90-94 calls EnterprisePageHero with no image/metrics/actions, rendering the heavier 'hero-dot-grid bg-zinc-50 dark:bg-[#09090B]' card (EnterprisePageHero.tsx L65). Sibling CollectionsPageTemplate.tsx L135 uses SectionHeader, so this is genuinely inconsistent (Jakob's). Valid but the lowest-severity P1 — it renders correctly and on-palette; pure consistency drift.
- `P2-pipeline-fitts-target` — CONFIRMED: pipeline chips L187-189 are px-3 py-2 text-xs (~32px tall, under 44px WCAG 2.5.5). Number badge L191 is an absolute decorative circle but lacks aria-hidden/pointer-events-none. Valid a11y target-size + focus-ring concern.
- `P2-pipeline-redundant-with-items` — CONFIRMED: pipeline (L176-205) re-renders items.map in the same catalog order with the same 1..n numbering already on the cards (L42) and in the graph — three parallel representations of one ordered set. Gated only on items.length>1, not on a real directed composition. Genuine Miller/Hick redundancy.
- `P2-metadata-row-grouping` — CONFIRMED: L97-122 mixes .chip.chip-neutral (category/difficulty), a bespoke 'rounded-full bg-zinc-100 ... text-[10px]' keyword tag, and an inline 'text-xs text-zinc-500' count sentence on one wrap line — three treatments for one fact family + glued body copy. 10px tags are below comfortable read size.

_Spec caveats:_ Every proposed edit composes existing, verified tokens/classes — SectionHeader (size xl/md, as, animate props all exist, SectionHeader.tsx L8-13), .stagger-grid (globals.css L2891), .chip/.chip-neutral (L299/L355), .btn/.btn-cta (L1341 min-height:2.875rem≈46px, L1410 with correct #DC2626 light / #F87171 dark), .catalog-card (L2254). No new globals.css needed; prefers-reduced-motion already honored (L3251). Minor caveats, none blocking: (1) The legend fix proposes solid/dashed/dotted 1px borders to encode type non-color-only — workable but RELATIONSHIP_TYPE_COLORS has up to ~7 distinct types appearing; three line styles cannot disambiguate that many, so the safer route the spec also offers (a .chip-neutral count pill labeled RELATIONSHIP_TYPE_LABELS + count, dropping hue entirely) should be the primary recommendation. (2) Spec keeps canvas edge/node colors (correct — sanctioned in-canvas exception), but note CATEGORY_COLORS (graphUtils L60-73) and backgroundColor='#09090b' inside the canvas are also off-palette yet permitted; do not let an implementer 'fix' those. (3) The pipeline 'gate behind a real directed-composition flag' edit requires a data-model field that does not currently exist on ContentCollection — so the implementable version is the simpler 'drop when it mirrors catalog order' branch, not the flag. (4) SectionHeader size='md' renders text-lg/xl which is correct H2 rank; pass animate={false} when placed inside an already-.reveal block to avoid double-observe, as the spec notes.

_Blockers:_
- CollectionGraph.tsx is SHARED by 6 routes (agents/mcp/skills/tools/podcasts collection-detail pages AND the skills/[slug] detail-page mini-graph). The legend change must be validated on every one — confirm canvas colors are untouched and the new monochrome legend still distinguishes all relationship types present per content type.
- CollectionDetailTemplate.tsx is the single template for all 5 content-type collection-detail routes; every structural edit (hero swap, heading promotion, grid restructure, pipeline gating) lands on all of them simultaneously. No per-type override exists, so re-check spacing (mt-6/mt-8 chain) and responsive layout at 1280/1024/768/375 across types.
- Moving the items grid out of the parent .reveal: the new top-level .stagger-grid must itself be observed by the IntersectionObserver or cards stay opacity:0 forever (stagger-grid > * defaults to opacity:0 until .revealed is added — globals.css L2891/L2897). Verify the observer targets .stagger-grid, not only .reveal.
- Swapping EnterprisePageHero (rounded-2xl dot-grid card, ~py-12→py-20) for a flat SectionHeader changes hero height and the surrounding vertical rhythm; the following mt-6 metadata block and mt-8 sections must be re-spaced to avoid a cramped top.


## platform-overview  (/aixcelerator)
Files: src/pages/aixcelerator/index.tsx
specSound: **true** · confirmed P1:2 P2:2 P3:3 · dropped:0

**Top P1 fixes:**
1. Promote the live-catalog metrics band to the visual peak: bump number from `text-xl` to `text-3xl sm:text-4xl font-bold tabular-nums`, increase cell padding, and frame it with a `size="md"` SectionHeader so the page's primary content leads (Serial Position + Von Restorff). Use bg-zinc-950 (not zinc-900) if darkening cells, to dodge the safety-net override.
2. Break the monotone six-grid wall by establishing a 3-tier emphasis system using ONLY size/surface-step/density: keep Core + Stacks as standard zinc-50/zinc-900 grids, and demote Roadmap + Cross-type-links to lighter inline rows so they recede (Aesthetic-Usability — systematize emphasis before anything else).
3. Collapse the duplicate hand-rolled hero kicker pill (lines 135-138) into SectionHeader's `kicker` prop so the hero matches all six section kickers' `text-label` uppercase coral-dot form (Jakob's / Uniform-Connectedness).

**Confirmed P1 detail:**
- `P1-hierarchy-monotone-grids` — Confirmed: lines 148,174,217,257,304,348 all render the identical `gap-px overflow-hidden rounded-xl border border-zinc-200 bg-zinc-200 dark:border-zinc-700 dark:bg-zinc-700` container with `bg-zinc-50 dark:bg-zinc-900` cells — six monotone grids, no emphasis tier; primary metrics get the same treatment as roadmap.
- `P1-metrics-not-primary` — Confirmed: line 161 number is `text-xl font-bold`, label `text-xs text-zinc-500` (159), icon chip `h-6 w-6` (156) — the page's stated primary content is the quietest element directly under the hero (Serial Position / peak violation).

**Confirmed P2:**
- `P2-redundant-hero-kicker` — Confirmed: hero hand-rolls a `bg-zinc-50 text-xs` non-uppercase pill with `bg-red-600` dot (135-138) while SectionHeader is called WITHOUT kicker (139-144); SectionHeader's own kicker (53-56) is `bg-zinc-100 text-label uppercase tracking-[0.14em]` + `bg-[#DC2626]` dot — diverges from all six section kickers.
- `P2-cta-secondary-contrast` — Confirmed and UNDERSTATED: secondary is `bg-white/90 text-zinc-900` (44); the globals.css `.dark .bg-white/90` safety-net (line 1192) repaints it `rgba(24,24,27,0.90)` dark zinc in dark mode, so dark-zinc `text-zinc-900` text sits on a near-band-colored dark fill — a genuine WCAG 1.4.3 dark-mode failure plus two competing CTAs.

_Spec caveats:_ The spec is overwhelmingly system-compliant: every edit composes existing zinc-scale tokens (bg-zinc-50/100/900/950), tabular-nums, text-3xl/4xl, transition-colors duration-150, the coral `#DC2626` small-dot exception, and SectionHeader's kicker — no invented tokens, no forbidden colors, no new CSS. It correctly mandates bg-zinc-950 (not zinc-900) to dodge the documented `.dark .bg-zinc-900` safety-net (verified at globals.css:1240). Issues: (1) The stagger-nesting edit lists 'three stagger sections (Core, Stacks, Modular layers)' but Modular layers (304) is a plain grid, not `.stagger-grid` — only Core and Stacks actually nest, so the edit scope is mis-stated. (2) The metrics-promotion edit and the roadmap/cross-type DEMOTION edit slightly conflict with src/pages/CLAUDE.md, which documents the unified gap-px grid as the intentional card pattern for this page; changing surface-step and collapsing roadmap to bordered rows is a deliberate deviation from a documented norm — defensible (hierarchy beats uniformity here) but should be acknowledged as overriding the page's stated 'unified grid' convention, not a bug fix. (3) The CTA secondary fix should also account for the `.dark .bg-white/90` safety-net override: moving to `bg-transparent border-white/30 text-white` is correct and actually REMOVES the latent dark-mode safety-net repaint bug, so the fix is sounder than the audit realized.

_Blockers:_
- EnterpriseCtaBand.tsx is a shared component rendered at the bottom of 15+ pages (per components/CLAUDE.md). Demoting its secondary button from solid to ghost is a GLOBAL visual change — must be regression-checked on every page that renders the band, and is arguably out of scope for a single /aixcelerator surface review; consider a page-local override prop instead of mutating the shared component.
- SectionHeader.tsx is also shared across the entire site; the spec only PASSES props to it (kicker) and changes no internals, so this is safe, but any temptation to alter its kicker styling would ripple site-wide.
- Demoting Roadmap + Cross-type-links away from the documented 'unified gap-px grid' pattern contradicts src/pages/CLAUDE.md's stated card pattern for this page — needs a doc update or explicit sign-off so a future reviewer does not 'fix' it back.


## podcasts-content  (/resources/podcasts)
Files: src/pages/resources/podcasts/index.tsx
specSound: **true** · confirmed P1:2 P2:5 P3:2 · dropped:0

**Top P1 fixes:**
1. P1-brand-beige-offpalette — replace all hardcoded warm-beige + text hexes (hero #F5F3EE/#1E1D1A line 313; sidebar #E8E5DE/#2A2824 line 597 = footer-surface reused off-label; chips #D4D1CA/#3D3A33 line 643; text #18181B/#71717A/#52525B/#A1A1AA) with zinc tokens / surface-panel / chip-neutral. Single biggest, highest-leverage divergence; makes the page a sibling of the other catalogs in one pass.
2. P1-a11y-target-size — bump search toggle h-9 w-9 (36px, line 243), all share/copy buttons h-8 w-8 (32px, 394/414/427/503/523/536) to h-11 w-11, sort tabs min-h-[40px]->[44px] (218,228), and surface a touch play affordance on list cards (remove 'hidden ... sm:flex' at 560 or add a visible Play control). WCAG 2.5.5 + a real mobile-can't-play functional gap.
3. P2-a11y-meta-contrast (treat as near-P1) — change the most-repeated body text on the page (date/duration/source spans 371/376/380/481/485/489) from text-zinc-400 to text-zinc-500 dark:text-zinc-400; light-mode zinc-400 ~2.3:1 fails AA and the safety net only fixes dark.

**Confirmed P1 detail:**
- `P1-brand-beige-offpalette` — Confirmed: hero bg-[#F5F3EE] dark:bg-[#1E1D1A] (line 313), sidebar bg-[#E8E5DE] dark:bg-[#2A2824] (597) which are EXACTLY .footer-surface colors (globals.css:1640 #E8E5DE / 1669 #2A2824 — footer surface reused off-label), chips bg-[#D4D1CA].../[#3D3A33] (643), separators [#D4D1CA]/[#4A473F] (634), and hardcoded text hexes #18181B/#71717A/#52525B/#A1A1AA (609,612,618,635,649). Off-palette per locked zinc+coral system. Biggest divergence.
- `P1-a11y-target-size` — Confirmed: search toggle h-9 w-9=36px (243); share/copy buttons h-8 w-8=32px (394,414,427,503,523,536); sort tabs min-h-[40px] (218,228) — all below 44px. List-card play button is 'hidden ... sm:flex' (560) so unplayable on touch; hero play (328) has no hidden so it IS available on mobile. WCAG 2.5.5 / Fitts violated.

**Confirmed P2:**
- `P1-a11y-tabs-semantics` — Confirmed the two Latest/Top links (216-235) have no aria-current/aria-selected; active state is color+weight only (bg-zinc-950 inverted) = WCAG 1.4.1 color-only state. Real but downgraded: full SSR nav for sort is by-design (getServerSideProps); the correct minimal fix is aria-current on the active anchor, NOT converting to a full ARIA tablist (over-engineering for link nav). P2.
- `P2-a11y-meta-contrast` — Confirmed text-zinc-400 used in BOTH modes for date/duration/source (371,376,380,481,485,489). Safety net at globals.css:1168 only remaps .dark .text-zinc-400 to #A1A1AA; light mode renders Tailwind #A1A1AA on white/zinc-50 ~2.3:1 — fails AA 4.5:1. zinc-500 fix (#71717A ~4.6:1) is correct.
- `P2-composition-reveal-stagger` — Confirmed standard violation: section.reveal (285) wraps div.stagger-grid (441); both apply opacity:0+translateY(32px). pages/CLAUDE.md and components/CLAUDE.md explicitly forbid nesting. NOTE: the audit's cited failure mode is partly wrong — Layout observer (line 937) querySelectorAll('.reveal:not(.revealed), .stagger-grid:not(.revealed)') DOES match the nested stagger-grid, so it gets .revealed (children not stuck invisible); the real harm is the parent transform context + competing reveals, not 'never fires'. Still a real standard breach.
- `P2-brand-card-hover-lift` — Confirmed: list rows use card-elevated (456) whose hover applies transform:translateY(-2px)+box-shadow:var(--shadow-lg) (globals.css:2041-2045). Locked standard wants catalog-card (globals.css:2254-2272, box-shadow:none, no lift). Only catalog surface with bouncing cards.
- `P2-contentIa-no-sectionheader` — Confirmed bare <h1>Podcasts</h1> (209-211), no kicker/description, does not use shared SectionHeader (component exists, supports size='xl'/kicker/title/description). AeoQuickAnswer renders above the H1 (201). Page Structure Standard requires .reveal hero w/ SectionHeader.

_Spec caveats:_ Spec is broadly sound — every edit composes EXISTING tokens/classes (surface-panel, catalog-card, chip-neutral, SectionHeader, input-premium, text-zinc-500, bg-zinc-50/900, border-zinc-200/700, animate-pulse) and invents no new values; newGlobalCssNeeded:false is correct. Concrete gaps to fix before handoff: (1) card-elevated->catalog-card edit keeps 'flex gap-5 p-5' but .catalog-card hardcodes 'display:flex; flex-direction:column' (globals.css:2256-2257) which will override and stack the row vertically — the edit MUST add 'flex-row' (and verify .catalog-card's 'overflow:hidden' at 2262 does not clip the hover-revealed share row or focus rings on the artwork/play button). (2) The hero re-skin to bg-zinc-900 is flagged safe by the spec, but the hero is NOT an inverted light/dark pair so this is fine; however surface-panel (used for sidebar) renders #141420 in dark (globals.css:565), not zinc-900 — acceptable but the implementer should confirm sidebar reads distinctly from the section-shell background in dark. (3) section-shell provides ONLY background+border-color (no border-width/padding) (globals.css:1530-1534); after stripping beige the hero/sidebar sit on a flat --surface-strong tint with the list cards on the same surface — verify catalog-card cards still separate visually in both modes (spec's risk note already calls this out, good). (4) aria-current fix for sort links is correct; do NOT escalate to role=tablist (would misrepresent link-based SSR navigation to AT). (5) The 'chip-neutral gives coral focus-visible for free' rationale is imprecise — chip-neutral (globals.css:355-377) defines no focus-visible; focus ring comes from the GLOBAL a:focus-visible (1334) since chips are anchors. Outcome is the same (focus is covered) but cite the right source.

_Blockers:_
- No hard blockers — all edits are confined to a single page file (src/pages/resources/podcasts/index.tsx); globals.css is referenced read-only (no global CSS change needed). SectionHeader is shared but only consumed here, not modified.
- Caveat: getServerSideProps and the SSR sort-link navigation must stay untouched (sort is by-design full-nav); only add aria-current — do not refactor to client-side tab state.
- Caveat: card-elevated->catalog-card needs flex-row added or the list row collapses to a vertical stack (catalog-card forces flex-direction:column). Verify in all 4 breakpoints (1280/1024/768/375) before merge.


## industries  (/industries)
Files: src/pages/industries/index.tsx
specSound: **true** · confirmed P1:2 P2:3 P3:3 · dropped:0

**Top P1 fixes:**
1. IND-1 (+IND-8): Replace hardcoded stroke/fill #DC2626 in iconProps and the two accent circles with token-driven coral so dark mode gets #F87171 — implement via container color:var(--pivot-fill) + currentColor on the SVGs (NOT the bare var() presentation attribute). Fixes brand + WCAG 1.4.11 non-text contrast in dark mode for all 14 icons.
2. IND-2: Repoint all three /resources/case-studies links (lines 67, 123, 188) to live routes (use distinct destinations to avoid duplicate CTAs), or gate body links behind isReleasePath() — stop sending users into a RELEASE_HIDDEN_PATHS surface.
3. IND-3 (+IND-4): Lighten the 10-vertical grid from heavy 16:9 media-frame cards to compact .catalog-card icon-row cards (h-10/w-10 icon tile, title, count, arrow), drop the redundant meta="Industry"; preserve .surface-interactive focus ring and aria-label for keyboard/SR parity.

**Confirmed P1 detail:**
- `IND-1` — Confirmed: iconProps line 52 hardcodes stroke:"#DC2626"; globals.css --pivot-fill is #DC2626 (:root L38) / #F87171 (.dark L171), so dark mode renders light-coral on dark/zinc-700 tile — off-brand + lower 1.4.11 non-text contrast.
- `IND-2` — Confirmed: three /resources/case-studies links (lines 67,123,188); that path is in RELEASE_HIDDEN_PATHS (Layout.tsx:250). /updates, /aixcelerator/skills, /aixcelerator/agents are all live. Real ship blocker — links into an unreleased surface.

**Confirmed P2:**
- `IND-3` — Confirmed: PremiumMediaCard size=sm renders 16:9 frame + 56px icon in zinc gradient block (PremiumMediaCard.tsx:48-55); heavy chrome for a nav grid. Legit scannability critique, but it is the sanctioned component (surface-panel, not .catalog-card) so the 'violates .catalog-card standard' framing overstates; weight/Von-Restorff argument stands.
- `IND-4` — Confirmed: meta="Industry" on all 10 cards (line 174) renders as repeated uppercase badge (PremiumMediaCard:83-89); redundant given the section title. Real, minor.
- `IND-5` — Confirmed: both SectionHeaders size="md" (lines 136,158) → text-lg sm:text-xl (SectionHeader:33), barely above card title text-base font-semibold. size="lg" branch exists and renders display scale. Valid hierarchy fix.

_Spec caveats:_ The spec composes only existing tokens/classes (--pivot-fill, .catalog-card, .surface-interactive, SectionHeader size=lg, zinc scale) and adds no new CSS — newGlobalCssNeeded:false is correct. Caveats for the implementer: (1) IND-1 SVG token risk is real and the spec acknowledges it: a bare CSS var in an SVG `stroke`/`fill` PRESENTATION ATTRIBUTE (stroke=\"var(--pivot-fill)\") will NOT resolve in many browsers — it only works via CSS. Implement via style/className driving `stroke`/`color:currentColor`, e.g. set the icon container `style={{ color: 'var(--pivot-fill)' }}` and use stroke=\"currentColor\"/fill=\"currentColor\" on the SVGs; do not ship the literal attribute form. Note the template already precedents token-driven accent (hero dot uses bg-[var(--trusted-fill)]). (2) IND-3 proposes a hand-rolled card markup rather than the PremiumMediaCard component — that is acceptable here (the compact catalog-card pattern is a deliberate swap) but the dev must wire focus-visible (.surface-interactive) and aria-label (the current card supplies aria-label={`Open ${title}`}) so keyboard/SR parity is preserved; the spec mentions surface-interactive but omits the aria-label — add it. (3) The spec does not address a pre-existing concern: SectionHeader applies internal `.reveal` classes (SectionHeader.tsx:53,59,63) and the `.stagger-grid` sits inside `<section class=\"reveal\">`, which conflicts with the documented 'never nest .stagger-grid inside .reveal' rule — pre-existing, not introduced by the spec, but bumping to size=lg keeps that nesting; worth a note, not a blocker. (4) Repointing case-studies links to /aixcelerator/agents is safe (live), but that makes hero primaryAction AND secondaryAction both point at /aixcelerator/agents — pick a distinct live destination for the secondary (e.g. /aixcelerator/skills or /updates) to avoid two identical CTAs (Von Restorff).

_Blockers:_
- IND-1/IND-8 token migration: do NOT use the literal stroke="var(--pivot-fill)" presentation-attribute form — CSS custom properties do not resolve inside SVG presentation attributes in most browsers. Must drive color via CSS (container style color:var(--pivot-fill) + currentColor on stroke/fill). Shipping the attribute form silently breaks the icon color.
- IND-2 repoint: /aixcelerator/agents is the spec's suggested target but the hero primaryAction already uses it; repointing the secondary there too yields duplicate CTAs. Choose a distinct live route (/aixcelerator/skills or /updates) for at least one of the three.
- PremiumMediaCard.tsx is a shared component used on multiple pages — the spec's IND-3 edit must stay local to industries/index.tsx (swap the call site to bespoke catalog-card markup); do NOT modify PremiumMediaCard itself or other surfaces regress. The spec lists PremiumMediaCard.tsx in some files arrays but the actual change is call-site only — keep it that way.


## request-demo  (/request-demo)
Files: src/pages/request-demo.tsx
specSound: **false** · confirmed P1:3 P2:2 P3:2 · dropped:0

**Top P1 fixes:**
1. Strip the forbidden purple/teal hexes (#4F2AA3/#F3EEFF/#2B0F63/#7B5CE0/#C4B3FF/#18233A/#008EA8) from request-demo.tsx — replace the hand-rolled kicker with SectionHeader's kicker prop, and recolor the Bullet/Step accent dots to bg-[#DC2626] and the Step number ring to the zinc chip pattern. Highest-leverage: it is the single biggest, fully-confirmed brand violation.
2. Add a visible coral focus ring to .input-premium:focus AND .dark .input-premium:focus in globals.css (box-shadow 0 0 0 3px rgba(220,38,38,0.28) / rgba(248,113,113,0.30), reusing the existing token), applied to BOTH definitions — fixes a genuine WCAG 2.4.7 failure where the default focus border is the identical color in light mode.
3. Remove the duplicate hero 'Submit demo request' button (request-demo.tsx 53-55); keep the form's own btn-cta as the sole primary, optionally replacing the hero affordance with a btn-secondary '#demo-request-form' jump anchor. Resolves the split-attention and premature-validation-on-unseen-form problem.

**Confirmed P1 detail:**
- `P1-brand-purple-teal` — Confirmed in request-demo.tsx: kicker div (line 38) uses border-[#4F2AA3]/15 bg-[#F3EEFF] text-[#2B0F63] + dark:#7B5CE0/#18233A/#C4B3FF; accent dot (line 39) and Bullet dot (line 101) use bg-[#008EA8] teal; Step number ring (line 110) uses #4F2AA3/#2B0F63/#7B5CE0/#C4B3FF. None are zinc or coral #DC2626/#F87171. Flagrant locked-system violation.
- `P1-duplicate-submit` — Confirmed: hero button (request-demo.tsx 53-55) is type=submit form="demo-request-form" labeled 'Submit demo request', identical to the form's own btn-cta submit (DemoRequestForm.tsx 376-382, same label). Two primary CTAs; the hero one sits above the form and fires validation on an unseen form. Real Von-Restorff/premature-submit issue.
- `P1-a11y-input-focus` — Confirmed: .input-premium:focus (globals.css 2447-2451 and the later 3051-3055) sets box-shadow:none and only border-color: var(--stroke). In light mode --stroke (#E4E4E7) === --neutral-stroke (#E4E4E7), so the focus border is literally the SAME color as default — zero visible change. Dark focus #71717A on #3F3F46 base is a weak <3:1 shift with no ring. Fails WCAG 2.4.7. The error-state class does add a focus shadow, but the default valid-field focus has none.

**Confirmed P2:**
- `P2-card-hover-lift` — Confirmed: Step() uses .card-elevated (request-demo.tsx 109); globals.css 2036/2041-2045 gives it box-shadow:0 1px 3px plus hover translateY(-2px)+shadow-lg. These four 'suggested flow' steps are non-interactive, so the lift is pure decoration violating the flat .catalog-card/.detail-section standard.
- `P2-no-cta-band` — Confirmed: request-demo.tsx ends at the grid (line 93) with no EnterpriseCtaBand; page-structure standard step 6 requires it. Legitimate IA/Peak-End gap, though this is a single-job conversion page where the form IS the closing CTA, so it is a soft standard miss not a defect.

_Spec caveats:_ Mostly sound — all replacement classes (SectionHeader kicker prop, btn-secondary, zinc border/bg/ring utilities, bg-[#DC2626], flat bordered card) compose EXISTING tokens and stay on the locked zinc+coral/Inter system; the coral focus-ring values reuse the exact tokens already at globals.css line 1290 and 387. Issues: (1) The EnterpriseCtaBand edit is under-specified — the literal '<EnterpriseCtaBand />' would NOT compile because the component requires 7 props (all required, no defaults, EnterpriseCtaBand.tsx 3-12); it must be expanded with kicker/title/description/primaryHref/primaryLabel/secondaryHref/secondaryLabel. (2) The focus-ring fix must hit BOTH .input-premium:focus blocks (globals.css 2447 and 3051); the later one at 3051 wins for light mode and has no .dark sibling, so naming only one pair risks the duplicate still zeroing box-shadow. (3) The nested-reveal edit rests on a false premise (children are NOT stuck at opacity:0 — Layout.tsx 936-938 observes all .reveal at any depth); the proposed fix is harmless but solves a non-bug. (4) bg-[#DC2626] as a literal is on-system, not an invented value (matches SectionHeader line 55).

_Blockers:_
- globals.css .input-premium is a SHARED primitive used by every form (newsletter, demo, internal-ops inputs and selects). Adding a coral focus ring is a GLOBAL change affecting all those surfaces — must be QA'd in both modes across forms, and applied to BOTH .input-premium:focus blocks (2447 and 3051) or the later definition will keep overriding box-shadow:none.
- DemoRequestForm.tsx is rendered on /request-demo and potentially other touchpoints; the required-asterisk and submit changes there ripple to any other mount of the form — verify there is only the one consumer before editing labels.
- EnterpriseCtaBand requires 7 props; appending it needs real copy + a valid secondary href (/aixcelerator), not the bare tag shown in the spec, or the build breaks.


## system-chrome  (global)
Files: src/components/Layout.tsx
specSound: **false** · confirmed P1:2 P2:5 P3:2 · dropped:0

**Top P1 fixes:**
1. CHR-03 — Raise light --stroke-focus from rgba(24,24,27,0.3) to ~0.6 or solid zinc-900 so the 3px focus ring clears 3:1 on #FAFAFA/#FFFFFF; single global token change in globals.css :root, highest a11y leverage (keyboard focus is near-invisible on the header today).
2. CHR-01 — Make --site-header-height responsive to the logo step (56/64/72px at base/sm/lg) OR cap the logo box so 64px stays true; fixes the sticky catalog rail + main tucking ~8px under the fixed header at lg. Re-verify .main-offset, Layout.tsx:1557 top + max-h calc.
3. CHR-04 — Repoint .footer-surface and footer separators (#E8E5DE/#2A2824/#D4D1CA/#4A473F) to zinc tokens (var(--surface-strong)/zinc-100 light, zinc-900 dark; var(--stroke) separators) to end the off-palette beige brand drift; token-driven, both modes.

**Confirmed P1 detail:**
- `CHR-01` — --site-header-height:64px (globals.css:26) is flat; header is py-2 (16px) + logo h-10/h-12/h-14 (40/48/56px) at mobile/sm/lg -> ~56/64/72px. Sticky rail top + max-h (Layout.tsx:1557) and .main-offset (258) all read 64px, so at lg the rail/main tuck ~8px under the ~72px header. Confirmed.
- `CHR-03` — --stroke-focus:rgba(24,24,27,0.3) light (globals.css:78) drives the 3px box-shadow ring (1331,1337). ~30% black over #FAFAFA composites to ~1.9:1 — below the 3:1 WCAG 1.4.11/2.4.11 non-text floor. Real.

**Confirmed P2:**
- `CHR-02` — h-10 w-10 (40px) confirmed at 1186,1199,1320,1341,1385,1761; h-9 (36px) at 1850; h-8 (32px) at 1508. But WCAG 2.5.8 AA floor is 24px (these PASS AA); 44px is 2.5.5 AAA/best-practice. Audit overstates the AA floor; still a real Fitts/target-size weakness.
- `CHR-04` — .footer-surface #E8E5DE/#2A2824 (1640,1669) and separators #D4D1CA/#4A473F (1688 inline, 1760, 1764) are warm beige/taupe, off the locked zinc+coral palette. Confirmed brand drift.
- `CHR-05` — Header CTA (1288) and drawer CTA (1469) use btn-primary (monochrome zinc invert); .btn-cta coral (globals.css:1410) is unused in chrome. Coral-for-CTA is exactly the sanctioned use, so this is a legit Von Restorff/hierarchy miss within the locked system (NOT a coral violation).
- `CHR-06` — PLATFORM_CHILD_BLUEPRINT yields 7 flat children (Overview/Agents/MCP/Skills/Ontology/Ecosystem/Solution Stacks) rendered ungrouped via meaningfulChildren.map (1146-1163). Real Hick's/Miller's chunking gap.
- `CHR-07` — Drawer parent is a navigating MobileLink with a decorative '→' (1419-1421); children always render (1422-1437); no aria-expanded, no disclosure button. Misleading affordance + WCAG 4.1.2 confirmed.

_Spec caveats:_ Mostly sound — every edit composes existing tokens (--site-header-height, --stroke-focus, --stroke, --surface-strong, --pivot-fill) and existing classes (.btn-icon, .btn-cta, section-heading utilities); no new design language, no forbidden colors, stays on Inter. Issues: (1) CHR-05 spec switches the header CTA to .btn-cta but btn-cta:hover carries a glow box-shadow (globals.css:1417-1419) that violates the locked no-glow/no-shadow-as-decoration rule — the spec only conditionally drops it ('if the no-glow rule is strict'); under this LOCKED standard the glow MUST be dropped, so make that mandatory, not optional. (2) The header CTA currently carries explicit h-10 (40px, 1288) while .btn sets min-height:2.875rem (46px); promoting to btn-cta keeps the h-10 override which can clamp it under the 44px floor — the CHR-02/CHR-05 edits must also drop the h-10/h-9 overrides on the CTA, not just on icon buttons. (3) CHR-01's responsive-token option (56/64/72px via media queries) is the safer fix, but --site-header-height and --stroke-focus are GLOBAL tokens consumed by .main-offset, .main-offset-home, every page top-gap and every site-wide :focus-visible — the spec flags this but the edit must be regression-checked on the homepage hero gap and all catalog pages, not just chrome. (4) CHR-04 footer recolor: globals.css has safety-net overrides (.dark .bg-zinc-900) — spec correctly steers to var(--surface-strong)/zinc tokens rather than bg-zinc-900 literals, acceptable; but .footer-callout-panel (#18181B/#27272A) is left untouched and is fine. No invented tokens; no template-shared component is restyled beyond Layout/globals.

_Blockers:_
- Layout.tsx + globals.css are GLOBAL chrome consumed by all 53+ pages; CHR-01 (--site-header-height) and CHR-03 (--stroke-focus) are token-value changes that ripple into .main-offset/.main-offset-home top-gap on EVERY page and every site-wide :focus-visible/.btn:focus ring — must be regression-checked across /, all /aixcelerator/* catalog pages, and detail templates at 1280/1024/768/375 in both modes, not just the header.
- CHR-01 responsive header-height interacts with the scroll-collapse header (site-header--compact, Layout.tsx:1179) which only changes bg/shadow, not height — but --header-height-full/compact are both 64px (155-156) and currently unused for layout; confirm nothing else depends on the flat 64px before making it responsive.
- CHR-05/CHR-02 must jointly remove the inline h-10/h-9 size overrides on the CTA and icon buttons or the class promotion (btn-cta / btn-icon=44px) will be clamped by the smaller explicit height.


## search-utility  (/search)
Files: src/pages/search.tsx
specSound: **true** · confirmed P1:3 P2:3 P3:2 · dropped:1

**Top P1 fixes:**
1. Replace the hardcoded purple-hex result-type badge (search.tsx:533) with 'chip chip-neutral text-xs' — removes the only forbidden-color violation, repeated on every result row.
2. Swap card-elevated for catalog-card on result rows (search.tsx:524) — removes shadow-as-decoration + translateY(-2px) hover-lift so results match the locked catalog cards they link to.
3. Make each result-group label an <h2> with section aria-labelledby (search.tsx:514-516) — restores h1->h2 heading hierarchy for screen-reader navigation (WCAG 1.3.1/2.4.6).

**Confirmed P1 detail:**
- `P1-brand-purple-badge` — search.tsx:533 literally hardcodes border-[#4F2AA3]/15 bg-[#F3EEFF] text-[#2B0F63] dark:bg-[#18233A] dark:text-[#C4B3FF] — forbidden arbitrary-hex purple on every result row; .chip-neutral (globals.css:355) is the correct zinc replacement.
- `P1-result-group-heading-semantics` — Group labels are non-semantic <div> (search.tsx:516); page has one h1 (SectionHeader as='h1') and zero h2s, so the 8 result categories are absent from the SR heading rotor. WCAG 1.3.1/2.4.6 genuinely violated. Card-title <div> inside <Link> is acceptable (link gets accessible name from text), so that sub-claim is weaker.
- `P1-card-elevated-hover-lift` — search.tsx:524 uses card-elevated; globals.css:2036-2044 confirms box-shadow + hover translateY(-2px)+shadow-lg. Locked spec mandates .catalog-card (globals.css:2254, box-shadow:none, no transform) for listing items. Genuine locked-card violation.

**Confirmed P2:**
- `P2-no-loading-skeleton` — getServerSideProps (search.tsx:368-387) blocks navigation across 6 endpoints w/ no client progress; StatePanel has an unused loading variant (StatePanel.tsx:16). Real Doherty gap, correctly P2. Note SSR nav means the 'searching' flag only helps if wired to router events.
- `P2-page-structure-standard-gaps` — Confirmed: refine panel uses detail-section (search.tsx:464) not surface-panel; grids are plain 'grid gap-3' (519) not stagger-grid; no EnterpriseCtaBand (component exists at src/components/EnterpriseCtaBand.tsx). All three deviate from pages/CLAUDE.md standard.
- `P2-empty-state-no-suggested-paths` — search.tsx:491-509 empty/no-result branches are bare StatePanels with prose only; STATIC_PAGES (71-142) hrefs exist to seed quick-links. Valid Tesler/Hick enhancement, correctly P2.

**Dropped/refuted:** P1-reveal-nesting-hides-hero→P3

_Spec caveats:_ Spec composes only existing tokens/classes (.chip+.chip-neutral, .catalog-card, .surface-panel, text-label, .btn/.btn-cta, .stagger-grid, StatePanel loading, EnterpriseCtaBand) and adds no new global CSS (newGlobalCssNeeded:false is correct). Issues: (1) The hero double-reveal edit is built on a false bug — nested .reveal children are observed and DO reveal (Layout.tsx:937 is a flat document query, not top-level-only). The edit is harmless (passing animate={false} to SectionHeader or removing outer .reveal is fine and removes a minor double-animation) but the stated rationale 'hero text can render permanently invisible' is wrong; do not gate ship on it. (2) stagger-grid edit: per CLAUDE.md never nest .stagger-grid inside a .reveal — the results wrapper at search.tsx:512 currently carries .reveal, so adding .stagger-grid to child grids WHILE the parent keeps .reveal would nest stagger-grid in reveal (the real anti-nesting violation). Spec acknowledges 'must remain top-level' but the concrete fix requires removing .reveal from the 512 wrapper first; flag this sequencing or the stagger cards mis-time. (3) .chip-neutral alone only sets border/bg/color (globals.css:355) — it REQUIRES the .chip base (globals.css:299) for padding/radius/shape; the spec's markup correctly uses 'chip chip-neutral', so OK, but any edit that drops 'chip' would render an unstyled span. (4) h2 styling 'text-sm font-semibold text-zinc-900 dark:text-zinc-100' is on-system (zinc only) and correct for both modes. No dark-mode regressions; no shared-template churn (search.tsx is a standalone page, not an ontology template).

_Blockers:_
- None hard. search.tsx is a standalone page (not a shared OntologyPageTemplate/CollectionsPageTemplate), so edits do not regress other routes.
- Sequencing constraint, not a blocker: the stagger-grid edit must be paired with removing .reveal from the results wrapper (search.tsx:512) to avoid the genuine .stagger-grid-inside-.reveal anti-nesting rule; do it in one change.
- Loading-state edit touches client routing (Next router events) on an otherwise pure-SSR page — verify it does not double-render with SSR results; low risk but the only edit with runtime behavior change.


---
**Totals across 12 surfaces:** P1=28 · P2=42 · P3=29
